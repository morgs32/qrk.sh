{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@zerospin/dispatch-worker",
  "targets": {
    "lib": {
      "inputs": ["default", "^default", "{projectRoot}/tsconfig.json"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
