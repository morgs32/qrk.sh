---
name: effect
description: >-
  Points to the local Effect source tree for documentation, idiomatic
  implementations, types, and source code. Use when working with Effect APIs,
  patterns, types, or when you need authoritative Effect implementation details.
---

# Effect

Look at the `./vendor/effect` folder for documentation, idiomatic effect implementations, types and source code.

Pull upstream Effect (squashed subtree):

```bash
git subtree pull \
  --prefix=vendor/effect \
  https://github.com/Effect-TS/effect.git \
  main \
  --squash
```
