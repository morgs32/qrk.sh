export enum PrimitiveKind {
  Boolean = 'boolean',
  Cursor = 'cursor',
  Date = 'date',
  Enum = 'enum',
  Integer = 'integer',
  Json = 'json',
  Number = 'number',
  ForeignKey = 'foreignKey',
  PrimaryKey = 'primaryKey',
  Ref = 'ref',
  Text = 'text',
}
