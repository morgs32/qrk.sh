/**
 * Give every clickable control an explicit pointer cursor.
 *
 * @bad Do not leave buttons or custom clickable controls with the default cursor.
 * @bad Do not limit pointer cursors to only the largest or most obvious actions.
 */
const clickableControlClassName =
  "cursor-pointer border text-foreground hover:bg-primary hover:text-primary-foreground";

void clickableControlClassName;
